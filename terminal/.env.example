# Base URL of the candle-agent api service.
# Dev default is http://localhost:8000 (the compose port); set this when the
# api runs elsewhere. The api must allow this origin - see CORS_ORIGINS.
VITE_API_URL=http://localhost:8000

# Symbol to load on first open, if the catalogue contains it.
VITE_DEFAULT_SYMBOL=AAPL
